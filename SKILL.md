# 一建机电学习复习系统 · WorkBuddy 技能包

> 一级建造师（机电工程管理与实务单科）**开箱即用**学习复习系统 · 基于 WorkBuddy 项目框架
> 版本 v1.0 | 2026-08-12 | 覆盖机电工程技术 / 法规标准 / 项目管理实务三大部分
> 形态：**纯前端单页应用（SPA）**，双击 `index.html` 即可运行，无需服务器与依赖安装。

---

## 一、系统定位

本技能包将「一建机电」备考需求收敛为**可直接运行**的 Web 应用，聚焦用户指定的五大核心功能：

| 用户需求 | 对应模块 | 能力 |
|----------|----------|------|
| 题库练习 | 题库练习 | 随机/章节/薄弱优先组卷、即时判分、解析、错题自动入库 |
| 章节复习 | 章节复习 | 按官方教材 3 篇 16 章树浏览考点、速记卡、一键本章练习 |
| 错题收藏 | 错题收藏 | 错题本（顽固排序）、收藏（★）、错题重做（连对 3 次消灭） |
| 模拟考试 | 模拟考试 | 限时/不限时、真实题型·分值·评分规则、成绩报告、PDF 导出 |
| 学习进度跟踪 | 进度跟踪 | 学习力指数、SVG 趋势图、章节掌握度、成就里程碑 |

---

## 二、框架目录结构（WorkBuddy 约定）

```
一建机电/
├── index.html              # 唯一入口（浏览器打开）
├── README.md              # 快速启动
├── SKILL.md               # 本文件
├── DESIGN.md              # 设计系统规范
├── assets/
│   ├── css/styles.css      # 视觉规范（深蓝 #0e1630 + 金 #e2a252）
│   └── js/
│       ├── data_mechanical.js  # 机电题库 + 章节考点，挂载 window.YJ_DATA.mechanical
│       ├── data_knowledge.js   # 考点知识库（高频/数字/规则/变动）
│       ├── data_lectures.js    # 名师讲课资源库
│       └── app.js          # 应用引擎 + 八大模块（IIFE，零外部依赖）
└── data/                  # 扩展资料目录（预留）
```

**编码规范**
- 数据层：纯浏览器全局对象（`window.YJ_DATA`），通过 `<script src>` 同步加载，兼容 `file://`。
- 应用层：单文件 IIFE，`"use strict"`，无 ES module / 无 fetch / 无 CDN，确保离线可运行。
- 持久化：浏览器 `localStorage`（key `yj_jd_state_v1`），结构含 `attempts / wrong / favorites / exams / studyDays / streak`。
- 图表：手写 SVG，无第三方库。
- 启动期对数据做字段归一化（`answer` 数组、`type` 映射、章节存在性校验），提升题库鲁棒性。

---

## 三、触发与运行

**用户说「打开一建机电」「刷机电题」「来套机电模考」「看下我的机电进度」等时：**
1. 定位到工作区 `C:\Users\dht\WorkBuddy\一建机电\`；
2. 直接用浏览器打开 `index.html`（或 `python -m http.server` 后访问）；
3. 如需扩充题库，按 `README.md` 的题对象格式向 `assets/js/data_mechanical.js` 的 `questions` 追加即可。

**评分规则（官方对齐）**
- 单选：选对 1 分，错选 0 分。
- 多选：全对 2 分；无错选时每正确选项 0.5 分；有错选 0 分。
- 案例：按采分点给分（模考中由用户自评满分 / 半分 / 0 分）。
- 及格线：机电实务 96 / 160（卷面满分 60%）。

---

## 四、内置数据资产

- **题库**：机电实务共 **68 题**（单选 50 + 多选 10 + 案例 8），覆盖 3 篇 16 章中 28 个章节，均含标准答案与解析；例题取自 2024 真题示例、模拟卷 #1 及高频 / 数字考点。
- **章节考点**：每章 `knowledge` 数组内置高频考点要点，支撑章节复习与速记卡。
- **考点知识库**：高频速记 / 数字考点 / 考试规则 / 教材变动 四类资料（`data_knowledge.js`）。
- **名师资源**：苏婷、唐琼、董美英、朱培浩、王克、侯杏莉、丁雷等名师课程导航（`data_lectures.js`）。

> 内置题为高质量样例，用于打通完整学习闭环；正式备考建议持续扩充至贴近真题规模（参考文件规划：20 套模拟卷、5 年真题结构化录入）。
